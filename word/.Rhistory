pi
install.packages("units")
?units
??units
library(units)
a = set_units(c(10,10), m/s
)
a
b =a
units(b) = make_units(km/h)
b
c = set_units(100, BTU/h)
d = c
units(d) = make_units(W)
d
library(measurements)
install.packages("measurements")
library(measurements)
measurements
names(conv_unit_options)
conv_unit_options$energy
conv_unit_options$power
